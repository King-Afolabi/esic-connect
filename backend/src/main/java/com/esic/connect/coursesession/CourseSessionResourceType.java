package com.esic.connect.coursesession;

/** Type de ressource du module {@code coursesession} concernée par un changement audité. */
public enum CourseSessionResourceType {
    COURSE_SESSION,
    ATTENDANCE_CHECKPOINT
}
