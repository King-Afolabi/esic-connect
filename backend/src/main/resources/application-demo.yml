# Profil "demo" : identique au profil "local" pour l'infrastructure
# (MySQL / Redis fournis par compose.yaml, valeurs via variables
# d'environnement du fichier .env), plus l'amorçage
# `DemoDataInitializer` et le mot de passe de démonstration.
#
# Ce profil NE désactive PAS la sécurité, N'UTILISE PAS `ddl-auto=create`
# et ne contient AUCUN secret en dur. À n'utiliser qu'en local.
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD}
  jpa:
    hibernate:
      # Flyway reste seul responsable du schéma.
      ddl-auto: validate

management:
  health:
    # Mailpit n'est pas critique pour la démonstration.
    mail:
      enabled: false

# Ce profil est celui réellement actif sur le déploiement public (Pi +
# tunnel Cloudflare) — il n'existe pas de profil "prod" séparé à ce
# stade (voir docs/03-architecture.md DEC-D01). La documentation OpenAPI
# et Swagger UI ne doivent donc pas être exposées ici : elles révèlent
# toute la surface d'API (endpoints, DTOs) à quiconque atteint le
# tunnel. Recette/démonstration interne : utiliser le profil `local`,
# où ces routes restent activées par défaut.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

app:
  demo:
    # Mot de passe des comptes de démonstration. Obligatoire, aucune
    # valeur par défaut : le démarrage échoue si absent ou trop court
    # (voir DemoDataInitializer). À définir dans votre shell, jamais dans
    # Git : `export ESIC_DEMO_PASSWORD=...` (≥ 12 caractères).
    password: ${ESIC_DEMO_PASSWORD}
    # Secret TOTP déterministe (base32), STRICTEMENT OPTIONNEL — vide par
    # défaut, aucun changement de comportement si absent. Permet à
    # `scripts/seed-demo.sh` et à la recette Playwright de franchir le
    # second facteur des comptes ADMIN/SUPER_ADMIN de démonstration sans
    # dépendre d'un secret généré aléatoirement à l'enrôlement (dette
    # T-19/T-20). Double garde : n'a d'effet que sous le profil `demo`
    # (comme ci-dessus) ET seulement si la variable est renseignée. Ne
    # doit JAMAIS être une valeur de production.
    totp-secret: ${ESIC_DEMO_TOTP_SECRET:}
