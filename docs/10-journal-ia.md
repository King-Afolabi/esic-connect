# Journal d’utilisation de l’intelligence artificielle

## Règles

- ne pas transmettre de données réelles ;
- ne pas transmettre de secrets ;
- vérifier chaque résultat ;
- conserver les prompts importants ;
- distinguer aide, décision et validation ;
- ne pas présenter une production IA comme preuve sans contrôle.

## Registre

| Date | Outil | Tâche | Données | Résultat | Vérification | Décision |
|---|---|---|---|---|---|---|
| 28/08/2026 | ChatGPT | Cadrage | Besoins exprimés | Document MD | Relecture humaine | Retenu avec corrections |
| 28/08/2026 | ChatGPT | Cahier des charges | Besoins fictifs | Exigences | Relecture humaine | Retenu |
| 28/08/2026 | ChatGPT | Architecture | Cahier des charges | Architecture | Relecture humaine | Retenu |
| 28/08/2026 | ChatGPT | Modèle de données | Architecture | MCD/MLD | Relecture humaine | Retenu |
| 30/08/2026 | Claude Code | Correction : idempotence du provisionnement des comptes de démonstration sur base MySQL persistante (`DefaultDemoAccountProvisioner`) | Code back-end, aucun secret | Resynchronisation du mot de passe et du statut `ACTIVE` des 4 comptes fictifs à chaque amorçage `demo`, hachage réécrit seulement si nécessaire | `./mvnw clean test` → 502 tests, 0 échec ; 3 tests ajoutés (resync, idempotence fonctionnelle, garde `@Profile("demo")`) | Retenu |
| 30/08/2026 | Claude Code | Grande tranche V10 : gestion de l'assiduité et reporting (migration `V10`, N points de contrôle par séance, jeton d'émargement par point de contrôle, présence manuelle / correction / annulation logique avec historique append-only, justificatif métier **sans fichier**, calcul de demi-journées, rapports séance / classe / apprenant + synthèse, export CSV avec neutralisation d'injection de formule, écrans Angular `/sessions` enrichi + `/my-attendance` + `/attendance-management`) | Code back-end + front-end, **données fictives uniquement**, aucun secret, aucune donnée personnelle réelle | Rapport de conception `docs/reports/ATTENDANCE_MANAGEMENT_DESIGN.md` (décisions figées, 6 divergences documentées vs docs/02 & docs/04) ; migration additive V1–V9 inchangées ; frontières Spring Modulith respectées (nouveaux ports `alternation.AlternationDirectory`, extensions de `CourseSessionDirectory` / `EnrollmentDirectory`) | Back-end `./mvnw clean test` → **548 tests, 0 échec** (502 → 532 → 545 → 548 au fil des passes de revue PR #22), `ModularityTests` vert, V10 appliquée sur MySQL 8.4 ; front-end `npm test` → **454 tests, 0 échec** (416 → 444 → 451 → 454), `npm run lint` OK, `npm run build` 482,24 kB (< seuil 500 kB) ; démonstration locale relevée en statuts HTTP | Retenu ; limites documentées (contexte d'alternance `UNKNOWN` non compté comme absence ; candidats à la saisie manuelle exposés via `GET /api/v1/sessions/{id}/attendance/candidates` et bornés à l'inscription valable le jour de la séance (2ᵉ passe PR #22) ; `TEACHER` hors rapports agrégés ; pas de test e2e Angular → Spring Boot) |
| 30/08/2026 | Claude Code | Conception (Checkpoint 0) de l'import CSV contrôlé des apprenants — EF-IMP-001 / EF-IMP-002, US-050 / US-051, RG-020 à RG-024, AC-004 à AC-006, TI-001 à TI-012 : nouveau module `studentimport`, migration additive `V11` (`student_import_job` / `_job_issue` / `_row` / `_row_issue` + `student_number_sequence`), téléversement multipart `.csv` non conservé, simulation sans écriture métier, confirmation transactionnelle idempotente avec re-validation, ports `identity.StudentAccountProvisioner` / `enrollment.StudentEnrollmentProvisioner` / extension `academic.ClassGroupDirectory`, écrans Angular `/students/import` | Documentation uniquement (aucun code) ; références docs/01 §8, docs/02 §10, docs/04 §16, docs/07 §9-10, docs/08 §9 ; **données fictives**, aucun secret | Rapport `docs/reports/STUDENT_CSV_IMPORT_DESIGN.md` (décisions arrêtées, 12 ambiguïtés listées à valider, 13 divergences assumées vs docs, découpage en 11 checkpoints) | Aucune commande exécutée (checkpoint de conception) ; frontières Spring Modulith vérifiées sur les ports existants ; relecture humaine attendue avant CP1 | En attente de validation — implémentation à suivre sur la même branche |
| 30/08/2026 | Claude Code | Révision R2 du rapport de conception de l'import CSV des apprenants : (1) « Livré » remplacé partout par « Prévu » / « Conçu » (aucun code n'existe) ; (2) `student_number` — colonne optionnelle + génération serveur atomique `ESIC-{annéeDébut}-{séquence}` via `student_number_sequence` (verrou de ligne, nouvelle tentative bornée, unicité SQL = autorité) ; (3) audit transactionnel précis de `EnrollmentPersister` (`REQUIRES_NEW`), `EnrollmentService` / `StudentProfileService` (via ce persister), `AccountInvitationService` (`AccountLifecycleEvent` → audit `REQUIRES_NEW`), création `UserAccount` + rôle, listeners d'audit `@EventListener` + `REQUIRES_NEW` ; (4) ports d'import redessinés pour une transaction unique (propagation `REQUIRED`, écriture directe, aucun événement d'audit synchrone, e-mail `AFTER_COMMIT`) + tests de rollback précis ; (5) reconfirmation — règle unique `APPLIED` → `200` + `alreadyApplied=true`, suppression d'`IMP_ALREADY_CONFIRMED` ; (6) données temporaires minimisées (colonnes typées, pas de JSON brut, purge courte configurable prototype `P7D`/`P30D`) ; (7) synthèse d'une page en tête | Documentation uniquement (aucun code) ; audit du code back-end existant, aucun secret lu | Rapport `docs/reports/STUDENT_CSV_IMPORT_DESIGN.md` R2 + `docs/CURRENT-STATE.md` + corps PR #23 | Aucune commande exécutée ; sémantique transactionnelle vérifiée par lecture de `EnrollmentPersister`, `EnrollmentService`, `StudentProfileService`, `AccountInvitationService`, `InvitationEmailListener`, `*AuditListener` | En attente de validation |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP1 (schéma uniquement)** : migration `V11__create_student_import_tables.sql` (tables `student_import_job` / `_job_issue` / `_row` / `_row_issue` + séquence `student_number_sequence` ; chaîne `ON DELETE CASCADE`, FK `RESTRICT` vers `user_account`, `CHECK` statut / gravité / action / tailles, `UNIQUE (job_id, row_number)`) ; module Spring Modulith `studentimport` (`package-info`, entités JPA `studentimport.internal` sur `shared.BaseEntity`, enums, repositories `JpaRepository` en lecture) ; test `StudentImportSchemaConstraintsTests`. Aucun endpoint, parsing, service, listener, port ni frontend (CP2+). | Code back-end + migration ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle (pas de H2) | 5 tables V11 additives (V1–V10 inchangées) ; module `studentimport` ; 19 tests `@DataJpaTest` | `./mvnw test -Dtest=StudentImportSchemaConstraintsTests` → **19 tests, 0 échec** ; `./mvnw clean test` (dont schéma jetable `esic_cp1_verify`) → **567 tests** (548 → 567, +19), `ModularityTests` vert, Hibernate `validate` OK, schéma en version 11 ; **7 échecs pré-existants** `AttendanceIntegrationTests` (`ATT_NOT_ENROLLED`), reproduits arbre ramené à `e8fd16d`, hors périmètre CP1, non corrigés | Retenu ; écarts documentés (entités/`package-info` avancés du CP2 car requis par le `@DataJpaTest` CP1 ; colonne `version` ajoutée aux 3 tables filles pour `BaseEntity` ; `row_number` cité — mot réservé MySQL 8) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP2 (socle interne du module `studentimport`)** : `StudentImportWeb` (constante `MANAGE_ROLES` + helpers `parseUuid` / `subject`), `StudentImportException` + `Kind` (21 valeurs) + `StudentImportExceptionHandler` (`@RestControllerAdvice(basePackageClasses = StudentImportWeb.class)`, mapping `IMP_*`, retraduction `MaxUploadSizeExceededException`), `StudentImportIssueCodes` (constantes `error_code`), `StudentImportProperties` (`@ConfigurationProperties(prefix = "app.import.student")` + `@Validated`) + `StudentImportConfig` (`@EnableConfigurationProperties`) + bloc `app.import.student` d'`application.yml`. Aucun parsing, aucune API, aucune simulation, aucun port, aucun événement, aucun frontend. Aucune migration. | Code back-end ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle | Socle transverse du module ; 7 tests unitaires purs ajoutés | `./mvnw test -Dtest='StudentImportPropertiesTests,StudentImportExceptionHandlerTests,StudentImportSchemaConstraintsTests,ModularityTests'` → **30 tests, 0 échec** ; `EsicConnectApplicationTests` vert (contexte complet avec le nouveau `@ConfigurationProperties`) ; `ModularityTests` vert (aucune dépendance inter-module ajoutée) | Retenu ; écart documenté (typage config + bloc `app.import.student` d'`application.yml` créés dès CP2 ; `spring.servlet.multipart.*` reste pour le checkpoint API) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP3 (lecture sécurisée du CSV)** : `RecognizedColumn` (11 colonnes du modèle réduit + en-têtes ignorés), `CsvFileGuard` (extension `.csv`, `Content-Type` toléré, rejet ZIP/OLE2/PDF/octet nul, taille bornée, UTF-8 strict, BOM retiré — fichier jamais écrit sur disque), `CsvParser` (RFC 4180 maison : guillemets, guillemet doublé, cellule multi-lignes, `CRLF`/`LF`, séparateur `,`/`;` auto-détecté, lignes vides ignorées non comptées, classification d'en-tête, `tooManyRows`/`noDataRows`), `CsvValueNormalizer` (trim/casse/téléphone/date deux formats/booléen tolérant/nom de fichier assaini/SHA-256/troncature `received_value`), `CsvRowNormalizer` → `NormalizedRow` (mapping par en-tête + indicateurs de forme + valeurs brutes conservées). Aucune règle métier, aucun accès base, aucun port, aucune API, aucune migration. | Code back-end ; **données fictives uniquement**, aucun secret ; composants purs (aucun contexte Spring) | Composants purs de lecture / normalisation ; 36 tests unitaires purs | `./mvnw test -Dtest='CsvFileGuardTests,CsvParserTests,CsvValueNormalizerTests,CsvRowNormalizerTests'` → **36 tests, 0 échec** ; aucune frontière de module modifiée | Retenu ; écart documenté (dé-duplication fichier + règles de champ déplacées au CP4, formatage de numéro au CP6 — évite du code mort) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP4 (validation, ports d'import, simulation)** : port `academic.ClassGroupDirectory.resolveForImport` + type scellé `ClassGroupResolution` ; port `identity.StudentAccountProvisioner` (+ impl `@Transactional REQUIRED`, publie `AccountInvitationIssuedEvent` jamais `AccountLifecycleEvent`) ; port `enrollment.StudentEnrollmentProvisioner` (+ impl écriture directe `saveAndFlush`, sans `EnrollmentPersister`, sans événement) ; `StudentImportFieldValidator` / `FileDuplicateDetector` / `PlannedActionResolver` / `StudentImportSimulationService` (persistance `student_import_*` uniquement). Aucune écriture métier, aucun e-mail, aucun événement d'audit, aucun contrôleur, aucune migration. | Code back-end (3 modules + `studentimport`) ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle | 3 ports publics + 3 impls confinées + moteur de simulation ; 37 tests (unitaires Mockito + `@DataJpaTest` + réflexifs + `@SpringBootTest`) | `./mvnw test -Dtest='StudentImportFieldValidatorTests,FileDuplicateDetectorTests,PlannedActionResolverTests,StudentImportProvisionerContractTests,ClassGroupResolveForImportTests,StudentImportSimulationIntegrationTests,ModularityTests,EsicConnectApplicationTests'` → **37 tests, 0 échec** ; invariant T1 vérifié (100 lignes → aucune écriture métier) ; `ModularityTests` vert (dépendances inter-modules limitées aux ports publics) | Retenu ; écarts documentés (génération de numéro laissée au CP6 car `student_number_sequence` appartient à `studentimport` ; structural BLOCKING → 4xx sans job ; filtre de job refusé pour un appelant non global, contrôle par ligne = autorité ; `SUSPENDED` ajouté aux comptes non exploitables) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP5 (API de simulation et consultation)** : `StudentImportController` (`POST` multipart → simulation, `GET` liste / détail / `rows` avec pagination + filtres) + `StudentImportQueryService` (décision fine de périmètre : `PEDAGOGICAL_MANAGER` = ses jobs, `403 IMP_JOB_FORBIDDEN` sinon) + `StudentImportSpecifications` / `StudentImportQuerySupport` (tri liste blanche `IMP_INVALID_SORT`, filtre `IMP_INVALID_FILTER`) + DTO `StudentImportResponses` sans `id` SQL + `PageResponse` local + bloc `spring.servlet.multipart` d'`application.yml` + `@Order(HIGHEST_PRECEDENCE)` sur `StudentImportExceptionHandler` (sinon `GlobalExceptionHandler` interceptait `StudentImportException` en 500). Aucun endpoint de confirmation / annulation, aucune migration. | Code back-end ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle | Couche HTTP de simulation + consultation ; 7 tests `@SpringBootTest` multipart | `./mvnw test -Dtest='com.esic.connect.studentimport.**,ClassGroupResolveForImportTests'` → **107 tests, 0 échec** (téléversement `201` sans `id` SQL, `IMP_MISSING_COLUMN` + `details`, ZIP → `415`, `rows` pagination + filtre, périmètre RP `403 IMP_JOB_FORBIDDEN`, `404` job inconnu, `400` tri/filtre invalides, matrice `401`/`403`) ; `ModularityTests` + `EsicConnectApplicationTests` verts | Retenu ; écart documenté (`cancel` déplacé du CP5 (rapport §15) au CP8 avec `confirm` — mutations d'état) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP6 (confirmation transactionnelle)** : `StudentNumberAllocator` (allocation atomique `student_number_sequence`, format `ESIC-{annéeDébut}-{NNNNN}`, borne → `IMP_STUDENT_NUMBER_EXHAUSTED`) ; `StudentImportConfirmationService` (`confirm` non transactionnel → `runConfirmation` `@Transactional` unique : verrou pessimiste `SELECT … FOR UPDATE`, idempotence `APPLIED` → `alreadyApplied=true` (T6), re-validation complète, application via ports `REQUIRED` `identity`/`enrollment`, numéro pré-alloué puis testé libre avant l'INSERT, `markApplied` ; toute exception → rollback total (T3), e-mail seulement `AFTER_COMMIT` (T4)) ; `StaleRevalidationPersister` (`@Transactional(REQUIRES_NEW)` — anomalies rafraîchies persistées après commit sans application, hors verrou, `student_import_*` uniquement). Aucun contrôleur HTTP, aucun événement d'audit, aucune migration. | Code back-end ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle | Confirmation transactionnelle + génération de numéro ; 13 tests (`@DataJpaTest` + `@SpringBootTest` rollback / concurrence) | `./mvnw test -Dtest='com.esic.connect.studentimport.**,ClassGroupResolveForImportTests,EsicConnectApplicationTests'` → **120 tests, 0 échec** ; 100 lignes → `APPLIED` + 100 comptes/profils/inscriptions/invitations/e-mails ; reconfirmation idempotente (TI-012) ; classe archivée → `STALE_SIMULATION` ; transfert conserve l'historique sans doublon de compte (TI-005/006) ; échec dernière ligne → rollback total, séquence intacte, 0 e-mail (§14.4) ; 2 confirmations concurrentes → 1 seul jeu de comptes (§14.6) ; `ModularityTests` vert | Retenu ; écarts documentés (`confirm` non transactionnel + `runConfirmation` via self-proxy pour séparer le commit du stale du signal d'exception ; `StaleRevalidationPersister` en `REQUIRES_NEW` sur métadonnées uniquement — sûr, strictement postérieur à une re-validation en lecture ; nouvelle tentative de numéro faite hors persistance ; test de schéma CP1 basculé sur années sentinelles) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP7 (audit et purge)** : `StudentImportChangeEvent` + `StudentImportChangeAction` (record public, aucune PII) publiés dans la transaction de simulation / confirmation ; `audit.internal.StudentImportAuditListener` **`@TransactionalEventListener(AFTER_COMMIT)` + `@Transactional(REQUIRES_NEW)`** (déviation volontaire du motif legacy — aucune trace si rollback, invariant T5) ; `StudentImportPurgeService` (`@Scheduled` + `@EnableScheduling` ; `purge()` : suppression en cascade des jobs `SIMULATED`/`EXPIRED` expirés + `CANCELLED` anciens, lignes filles des jobs `APPLIED` anciens supprimées, agrégats conservés, `student_number_sequence` jamais purgée). Aucun contrôleur HTTP, aucune migration. | Code back-end ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle | Événement d'audit + listener transactionnel dédié + purge planifiée ; 5 tests `@SpringBootTest` | `./mvnw test -Dtest='com.esic.connect.studentimport.**,ClassGroupResolveForImportTests,ModularityTests,EsicConnectApplicationTests,com.esic.connect.audit.**'` → **127 tests, 0 échec** ; simulation → 1 ligne `STUDENT_IMPORT_SIMULATED` sans PII ; confirmation committée → 1 ligne `_CONFIRMED` ; confirmation en rollback → **0 ligne d'audit** (§14.4) ; purge cascade + trim `APPLIED` (§14.3) ; `ModularityTests` vert | Retenu ; écart documenté (déviation `AFTER_COMMIT` + `REQUIRES_NEW` assumée pour cet unique listener ; `@EnableScheduling` global déclaré dans `StudentImportConfig`) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP8 (API de confirmation et cycle de vie)** : `POST /api/v1/student-imports/{id}/confirm` → `200` `ConfirmationResultResponse` (reconfirmation `APPLIED` → `200` + `alreadyApplied=true`, T6 ; erreurs `409 IMP_NOT_CONFIRMABLE`/`IMP_STALE_SIMULATION`/`IMP_SIMULATION_EXPIRED`/`IMP_JOB_CANCELLED`, `403 IMP_CONFIRM_FORBIDDEN`) ; `POST .../{id}/cancel` → `204` (`409 IMP_JOB_NOT_CANCELLABLE`, `403 IMP_JOB_FORBIDDEN`, `404`) ; `StudentImportConfirmationService.cancel` (`@Transactional`, contrôle fin de périmètre, `markCancelled` + `StudentImportChangeEvent(CANCELLED)`). Aucune migration. | Code back-end ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle | 2 endpoints de cycle de vie + méthode `cancel` ; 5 tests `@SpringBootTest` HTTP | `./mvnw test -Dtest='com.esic.connect.studentimport.**'` → **122 tests, 0 échec** ; `confirm` → `200`/`APPLIED` ; reconfirmation `alreadyApplied=true` (TI-012) ; non confirmable / expiré / annulé → `409` typés ; `cancel` `SIMULATED` → `204`, `APPLIED` → `409` ; matrice `401`/`403` + périmètre RP ; 2 `confirm` HTTP concurrents → 1 seul jeu de comptes ; `ModularityTests` vert | Retenu ; écart documenté (`cancel` regroupé avec `confirm` — mutations d'état) ; correctif : e-mails randomisés dans le test de cycle de vie (base de test partagée) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP9 (interface Angular)** : routes `/students/import` + `/students/import/:publicId` (garde `roleGuard` 4 rôles, hors du sous-arbre `/students` qui exclut le `PEDAGOGICAL_MANAGER`) + entrée nav « Import apprenants » ; `StudentImportApiService` (une méthode par endpoint, `simulate` en `FormData` multipart, fichier jamais lu côté navigateur) ; `student-import.models.ts` (miroir des DTO) ; `student-import-errors.ts` (liste blanche explicite `KNOWN_IMP_CODES`, jamais `startsWith`) ; `StudentImportHome` (téléversement + refus client `.csv`/taille + imports récents) ; `StudentImportReview` (cartes de synthèse, anomalies globales, table des lignes filtrable/paginée serveur, confirmation en ligne avec récapitulatif, `IMP_STALE_SIMULATION` → rechargement + blocage, annulation, perte du contexte de rôle → actions masquées + réponse tardive ignorée). Aucun fichier back-end, aucune dépendance npm. | Code front-end Angular ; **données fictives uniquement**, aucun secret ; JWT / contexte de rôle en mémoire seule, rien en `localStorage`/`sessionStorage` (asserté) | 2 écrans + service + modèles + mapping d'erreurs ; 17 tests Vitest | `npm test` → **471 tests, 0 échec** (454 → 471) ; `npm run lint` → « All files pass linting » ; `npm run build` → bundle initial **483,26 kB** (< seuil 500 kB) ; `./mvnw` non ré-exécuté (aucun fichier back-end modifié) | Retenu ; écart documenté (route `/students/import` déclarée hors du sous-arbre `/students` pour ouvrir l'import au `PEDAGOGICAL_MANAGER`) |
| 31/08/2026 | Claude Code | Import CSV apprenants — **checkpoint CP10 (recette complète et traçabilité)** : `StudentImportRecetteTests` (5 : TI-010 fichier trop volumineux → `413 IMP_FILE_TOO_LARGE` ; TI-007 partie ligne — `PEDAGOGICAL_MANAGER` hors périmètre → lignes `IMP_CLASS_OUT_OF_SCOPE`, job non confirmable ; invariant T5 par effet observable — 0 ligne d'audit `ACCOUNT_INVITATION_ISSUED` mais e-mail envoyé + 1 ligne `STUDENT_IMPORT_CONFIRMED` ; §14.6 — deux imports aux e-mails recoupés → aucun doublon de compte ; §14.5 — `401` anonyme sur les 6 endpoints) ; correctif `spring.servlet.multipart.resolve-lazily: true` + handler `MultipartException` (sinon dépassement de taille en 500 avant résolution du contrôleur). Aucune migration ; V1–V11 inchangées. | Code back-end (recette) + config ; **données fictives uniquement**, aucun secret ; base MySQL de test réelle ; front-end ré-exécuté | Recette des cas non couverts + vérification globale | `./mvnw clean test` → **BUILD SUCCESS, 682 tests, 0 échec, 0 erreur** (81 classes, `ModularityTests` vert, schéma en version 11) ; `AttendanceIntegrationTests` → 25 tests, 0 échec — **les 7 échecs pré-existants notés au CP1 ne se reproduisent plus** (base de branche = fusion CP1 PR #24 ; aucun fichier `attendance`/`enrollment`/`coursesession` modifié par CP2–CP10) ; `npm test` → **471 tests, 0 échec** (454 → 471), `npm run lint` OK, `npm run build` **483,26 kB** (< 500 kB) ; TP-004 : simulation et confirmation d'un fichier de 100 apprenants < 1 s chacune sur MySQL 8.4 local ; invariants T1–T6 couverts par des tests nommés | Retenu ; limites documentées (pas de test e2e Angular → Spring Boot ; `UPDATE_PROFILE` borné aux champs d'alternance + téléphone ; exceptions collectives d'alternance hors périmètre ; purge à cadence quotidienne fixe ; dette transactionnelle globale de l'audit des autres modules non traitée — le chemin d'import y échappe via T5) |
| 31/08/2026 | Claude Code | **Finalisation — checkpoint F1 (audit de l'état réel)** : audit vérifiable de ce qui est réellement implémenté / testé / démontré / documenté / manquant sur `chore/project-finalization-v2` (branche alignée sur `main` à `9c5affa`). Matrice fonctionnelle par tranche, matrice des exigences (EF-*, RG-*, AC-*, technique, sécurité/RGPD, perf, a11y, doc), inventaire endpoints/routes, état des tests, sécurité & dépendances, audit documentaire, backlog `FINAL-001..030`, séquence F2→F6, critères de fin de projet. Aucune fonctionnalité métier, aucune migration, aucun changement de dépendance. | Lecture du dépôt (code back/front, configs, workflows, docs) ; exécution des suites de tests et de l'audit npm ; **aucun secret lu ni affiché**, aucune donnée personnelle | `docs/reports/PROJECT_FINAL_AUDIT.md` (nouveau) + entrée journal + bannière de renvoi en tête de `docs/CURRENT-STATE.md` | `./mvnw clean test` → **682 tests, 0 échec, BUILD SUCCESS** ; `npm test` → **471 tests, 0 échec** ; `npm run lint` OK ; `npm run build` **483,26 kB** (< 500 kB, 2 avertissements de template non bloquants) ; `npm audit` → **0 vulnérabilité** ; `git log --all -- .env` vide ; frontières de modules vérifiées ; endpoints et routes recensés à la main | Retenu ; principaux constats : `CURRENT-STATE.md` et `09-matrice-rncp.md` désynchronisés (V10 + import CSV décrits « non fusionnés » alors qu'ils sont sur `main`), aucun `README.md` racine, import planning + publication + création des séances non implémentés (parcours prioritaire incomplet), CORS et en-têtes de sécurité HTTP absents, aucun scan de dépendances en CI |
| 31/08/2026 | Claude Code | **Finalisation — checkpoint F3 (qualité, tests, exploitabilité)** sur `chore/project-finalization-f2-f6`. Aucune règle métier modifiée, aucune migration. (FINAL-016) correction des avertissements de template `NG8107` / `NG8102` : `selectedCheckpoint` typé `CheckpointView \| null` + nouveau `computed` `manualTargetLabel` dans `session-detail.ts`, expression simplifiée dans `session-detail.html`. (FINAL-009) 2 tests JUnit `@Tag("perf")` — `StudentImportSimulationPerfTests` (simulation d'un import de 100 lignes) et `AttendanceTokenPerfTests` (émission + rotation d'un jeton contre Redis réel) — **exclus** du `mvn test` par défaut via `maven-surefire-plugin` + propriétés `test.groups` / `test.excludedGroups` + profil `-Pperf` ; résultats consignés dans `docs/reports/PERF_NOTES.md` (mesure indicative, aucun seuil CI, aucune garantie contractuelle). (FINAL-020) `axe-core` ajouté en `devDependencies`, helper `frontend/src/testing/axe.ts` (périmètre jsdom : pas de contraste), 2 specs `*.a11y.spec.ts` (login, émargement apprenant). (FINAL-018) `scripts/dump-openapi.sh` (export OpenAPI **au runtime**, aucun plugin de build ajouté ; `docs/openapi.json` ajouté à `.gitignore`) + note dans `README.md`. (FINAL-015) `docs/07-securite-rgpd.md` §14 : tableau « purge réelle vs cible » — seule la purge de l'import CSV est implémentée, le reste est `DOCUMENTATION_ONLY`, pas de mention « conforme RGPD ». (FINAL-021) `docs/reports/TEST_ISOLATION_DECISION.md` : migration Testcontainers **différée** (blast radius sur 47 classes de test + 29 `@TestConfiguration` imbriquées, risque sur les invariants transactionnels), contournement local documenté ; commentaire périmé d'`application-test.yml` corrigé. | Code back-end (tests + config Maven) + front-end (fix + tests) + scripts + docs ; **données fictives uniquement** (`example.test`), aucun secret lu ni affiché ; base MySQL / Redis locales | `session-detail.{ts,html}`, `backend/pom.xml`, `backend/src/test/resources/application-test.yml`, 2 tests perf back-end, `frontend/src/testing/axe.ts`, 2 specs a11y, `frontend/package.json` + `package-lock.json` (ajout `axe-core`, normalisation du lock par npm 11 — aucune version existante modifiée), `scripts/dump-openapi.sh`, `.gitignore`, `docs/07-securite-rgpd.md`, `docs/reports/PERF_NOTES.md`, `docs/reports/TEST_ISOLATION_DECISION.md`, `README.md`, `docs/CURRENT-STATE.md`, `docs/10-journal-ia.md` | `./mvnw clean test` → **BUILD SUCCESS, 682 tests, 0 échec** (perf exclu ; `ModularityTests` vert ; 3:06 min) ; `./mvnw -Pperf test -Dtest='StudentImportSimulationPerfTests,AttendanceTokenPerfTests'` → **2 tests, 0 échec** — simulate(100) p50 **636,5 ms**, token issue()+rotation p50 **2,88 ms** ; `npm test -- --watch=false` → **55 fichiers, 475 tests, 0 échec** (471 → 475) ; `npm run lint` OK ; `npm run build` **483,26 kB**, **0 avertissement `NG810*`** ; `npm audit` → **0 vulnérabilité** ; `npm ci` reproduit proprement (608 paquets) ; `git diff --check` propre | Retenu ; limites : mesures perf mono-machine (M2, 16 Go, MySQL/Redis localhost) et indicatives ; `axe-core` en jsdom ne couvre ni contraste ni zoom ni parcours lecteur d'écran réel ; Testcontainers non fait (dette assumée) ; pas de purge outillée hors import CSV |
| 31/08/2026 | Claude Code | **Finalisation — checkpoint F2 (vérité documentaire)** sur `chore/project-finalization-f2-f6` (base `main` = `e44ccb1`). Aligner la documentation sur le code fusionné : création de `README.md` racine (pitch, périmètre livré / non livré, architecture réelle, prérequis, installation, `.env`, lancement Docker/back/front, tests, comptes démo sans mot de passe, dépannage, décision explicite d'exclusion du planning) ; réécriture courte de `docs/CURRENT-STATE.md` (12 modules réels, V1–V11, capacités `IMPLEMENTED_AND_TESTED` / `PARTIAL` / `HORS_PÉRIMÈTRE_ASSUMÉ`, résultats du run F1) ; archivage verbatim de l'ancien contenu dans `docs/reports/PROJECT_HISTORY.md` (FINAL-023) ; bloc « état réel d'implémentation » en tête de `docs/03-architecture.md` §7 (12 modules réels, `planning`/`claim`/`ai`/`iot`/`reporting`/`justification` = cible non implémentée) + correction du « Différé » §7.4 ; `docs/09-matrice-rncp.md` — TR-023 marquée fusionnée (PR #22 `35bd04b`), nouvelle ligne **TR-024** (import CSV, module `studentimport`, PR #23-25, tests `StudentImport*`), TR-003 pointée sur `studentimport`, TR-004/005 annotées « planning : NON IMPLÉMENTÉ », note sur les totaux de tests périmés ; addendum de réduction de périmètre dans `docs/01` §23.5 et `docs/02` §4.5.1 ; entrée journal F2. **Aucun fichier de code, aucune migration, aucun changement de dépendance.** | Lecture du dépôt (modules `package-info`, migrations, contrôleurs REST, `app.routes.ts`, `compose.yaml`, `.env.example`, workflows CI, `application-demo.yml`, `DemoDataInitializer`) ; **aucun secret lu ni affiché**, données fictives (`example.test`) | `README.md` (nouveau), `docs/CURRENT-STATE.md` (réécrit), `docs/reports/PROJECT_HISTORY.md` (nouveau, archive), `docs/03-architecture.md` §7, `docs/09-matrice-rncp.md` §6, `docs/01-cadrage.md` §23.5, `docs/02-cahier-des-charges.md` §4.5.1, `docs/10-journal-ia.md` | Vérification des liens et commandes du `README` (tous les fichiers cités existent ; `docker compose` / `./mvnw` / `npm` / `scripts/*` réels) ; `git diff --check` propre ; diff limité aux fichiers `*.md` ; aucune suite de tests ré-exécutée (F2 = documentaire, la base de tests est celle du run F1) | Retenu ; le module `planning` reste `HORS_PÉRIMÈTRE_ASSUMÉ` (décision documentée, non implémentée) ; les totaux de tests seront re-mesurés à F3/F5/F6 et consignés dans `docs/reports/PROJECT_FINALIZATION_REPORT.md` |

## Modèle d’entrée

```markdown
## [DATE] — [OUTIL]

### Objectif

[Description]

### Données envoyées

- [Données]
- Classification : fictive/anonymisée/publique

### Prompt résumé

[Résumé]

### Résultat

[Résultat]

### Vérifications

- [Test]
- [Source]
- [Relecture]

### Décision

- Accepté
- Corrigé
- Rejeté

### Limites

[Limites]
```

## Responsabilité

Les assistants IA :

- proposent ;
- accélèrent ;
- expliquent ;
- génèrent des brouillons.

Le candidat :

- choisit ;
- vérifie ;
- teste ;
- assume les décisions ;
- explique le résultat au jury.